<% provide(:description, "「最安ドメイン比較」のお問い合わせページです。ご意見・ご質問等はこちらからお受けいたします。") %>
<% provide(:title, "お問い合わせ") %>
<h1>お問い合わせ</h1>
<%= render 'layouts/flash' %>
<%= form_with(model: @contact, local: true) do |form| %>
<div class="form-group">
  <label for="email">メールアドレス</label>
  <%= form.text_field :email, class: "form-control", id: "email", placeholder: "name@example.com" %>
</div>
<div class="form-group">
  <label for=name">お名前</label>
  <%= form.text_field :name, class: "form-control", id: "name", placeholder: "お名前" %>
</div>
<div class="form-group">
  <label for="message">お問い合わせ内容</label>
  <%= form.text_area :message, class: "form-control", id: "message" %>
</div>
<p>送信された情報は<%= link_to 'プライバシーポリシー', policy_path %>に基づき管理されます。</p>
<%= form.submit '送信', class: "btn btnMain" %>
<% end %>
